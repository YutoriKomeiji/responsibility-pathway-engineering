name: MCP stdio check

on:
  pull_request:
    paths:
      - "rpe_kernel/**"
      - "scripts/check_mcp_stdio.py"
      - "pyproject.toml"
      - ".github/workflows/mcp-stdio-check.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
      - run: python -m pip install .
      - run: python scripts/check_mcp_stdio.py
